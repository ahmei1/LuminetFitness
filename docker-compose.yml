services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: luminet
      POSTGRES_PASSWORD: luminet
      POSTGRES_DB: luminet
    volumes:
      - luminet_pg:/var/lib/postgresql/data

volumes:
  luminet_pg:
